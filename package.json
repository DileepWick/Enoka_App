{
  "dependencies": {
    "vite": "^6.0.1"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "preview": "cd frontend && vite preview",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm i && cd frontend && npm i && npm run build && cd .. && npm update -g npm"
  }
}

