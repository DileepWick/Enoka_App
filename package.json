{
  "name": "vite-node-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "start": "concurrently \"npm start --prefix backend\" \"npm run preview --prefix frontend\"",
    "heroku-postbuild": "npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}