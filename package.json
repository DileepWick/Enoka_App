{
  "dependencies": {
    "vite": "^6.0.1"
  },
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm i && npm install concurrently && cd frontend && npm i && cd ../backend && npm i && cd ..",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  }
}

