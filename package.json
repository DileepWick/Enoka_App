{
  "dependencies": {
    "vite": "^6.0.1"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "preview": "cd frontend && vite preview",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm i && cd backend && npm i && cd .. && npm update -g npm"
  }
}

