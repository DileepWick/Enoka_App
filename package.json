{
  "dependencies": {
    "vite": "^6.0.1"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "preview": "cd frontend && vite preview",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm i && npm install concurrently && cd frontend && npm i && npm run build && cd ../backend && npm i && cd ..",
    "dev": "concurrently \"npm start --prefix backend\" \"npm vite preview --port $PORT --host --prefix frontend\""
  }
}

